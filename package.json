{
  "name": "purrooser",
  "description": "Web browser with Python",
  "scripts": {
    "setup:unix": "python3 -m venv venv",
    "run:unix": "source venv/bin/activate; pip install -r requirements.txt; cd core; python -m main; cd ..; deactivate;",
    "bootstrap:unix": "#!/bin/bash; source venv/bin/activate; pip install -r requirements.txt; deactivate;",

    "run:win": "cd core && python -m main && cd ..",
    "bootstrap:win": "pip install -r requirements.txt;",

    "compile:ts": "tsc",
    "compile:scss": "sass --update ui:ui",
    "compile": "npm run compile:ts && npm run compile:scss",

    "unix:big": "npm install && python3 -m venv venv; source venv/bin/activate; pip install -r requirements.txt; npm run compile:scss; npm install -g tsc; tsc; cd core; python -m main; cd ..; deactivate;",
    "win:big": "npm install && pip install -r requirements.txt && npm run compile:scss && npm install -g typescript; tsc && cd core && python -m main; cd .."
  },
  "author": "Thoq",
  "license": "MIT",
  "devDependencies": {
    "@types/jquery": "^3.5.31",
    "sass": "^1.79.4",
    "scss": "^0.2.4",
    "typescript": "4.9.5",
    "tsc": "^2.0.4"
  }
}
