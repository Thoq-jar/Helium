#ifndef UTILS_HH
#define UTILS_HH

#include "../Main.hh"

class Utils {
public:
  static void Alert(const wxString &title, const wxString &message);

  static void GetSearchEngine();
};

#endif //UTILS_HH
